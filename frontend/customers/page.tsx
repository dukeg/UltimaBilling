'use client';
import { useEffect, useState } from 'react'; import { Shell } from '@/components/Shell'; import { api } from '@/lib/api';
export default function Customers(){ const [rows,setRows]=useState<any[]>([]); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const load=()=>api('/customers').then(setRows); useEffect(load,[]); async function add(){await api('/customers',{method:'POST',body:JSON.stringify({name,email})}); setName('');setEmail('');load()} return <Shell><h1 className="text-3xl font-bold mb-6">Customers</h1><div className="card mb-6 grid md:grid-cols-3 gap-3"><input className="input" placeholder="Name" value={name} onChange={e=>setName(e.target.value)}/><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn" onClick={add}>Add customer</button></div><div className="card">{rows.map(r=><div className="py-3 border-b" key={r.id}>{r.name} <span className="text-slate-500">{r.email}</span></div>)}</div></Shell> }
